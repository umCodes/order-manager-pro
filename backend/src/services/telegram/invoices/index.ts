/**
 * Invoice messages for the fulfillment channel: `messageText` decides how an
 * invoice reads, `notifications` decides when it's sent, replied to, or edited.
 */
export * from "./messageText.js";
export * from "./notifications.js";
