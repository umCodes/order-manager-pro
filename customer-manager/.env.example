# Base URL of the order-manager-pro backend API this app talks to.
# Points at the same backend as the main frontend — this app only adds a
# focused UI on top of the existing customer endpoints, no new backend.
VITE_API_BASE_URL=http://localhost:3000/api
